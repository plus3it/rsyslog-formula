# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: rsyslog
  rootgroup: root
  config: '/etc/rsyslog.conf'
  service:
    name: rsyslog
  subcomponent:
    config: '/etc/rsyslog-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
